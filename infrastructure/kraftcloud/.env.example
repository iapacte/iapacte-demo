# KraftCloud / Compose Environment Variables
# Copy to .env and adjust before deploying

# Public URL to expose to the client bundle
VITE_SERVER_URL=https://your-domain.com

# SSR runtime configuration
NODE_ENV=production
PORT=3000
NITRO_PORT=3000
