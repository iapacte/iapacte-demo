{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|MultiEdit|Write",
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.tool_input.file_path' | { read file_path; pnpm biome check --write --no-errors-on-unmatched --diagnostic-level=error  \"$file_path\"; }"
					}
				]
			}
		]
	}
}
