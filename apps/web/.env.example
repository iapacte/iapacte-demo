# Backend API URL exposed to the browser bundle
# - Development: leave unset to use Vite's proxy (keeps same-origin cookies)
# - Docker (local): set to http://localhost:8080 at build-time (compose does this)
# - Cloud/Fly: set via fly.toml to your public API (e.g., https://api.aipacto.com)
# VITE_SERVER_URL=
